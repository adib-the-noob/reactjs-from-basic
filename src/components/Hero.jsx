
const Hero = (props) => {
    return (
        <div>
            <h1>Hero Component</h1>
            <button onClick={props.ButtonClick}>Submit</button>
        </div>
        );
};

export default Hero;